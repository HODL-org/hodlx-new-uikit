import { ReactNode } from "react";
import { RenderResult } from "@testing-library/react";
export declare const renderWithTheme: (component: ReactNode, theme?: DefaultTheme) => RenderResult;
export declare const setupMockIntersectionObserver: () => void;
